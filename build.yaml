targets:
  $default:
    builders:
      json_serializable:
        options:
          explicit_to_json: true
          field_rename: snake
          include_if_null: false
          create_factory: true 
          any_map: true  # This allows us to work with Map<String, dynamic>
